language: node_js
node_js:
  - "12"
install:
  - yarn
cache:
  yarn: true
script: bundle exec yarn build
before_install:
- sudo apt-get install lftp
after_success:
- lftp -e "mirror -R --delete ./dist /www/viewtube.eu" -u ${FTP_USER},${FTP_PASSWORD} sl261.web.hostpoint.ch
