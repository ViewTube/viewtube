{
  "name": "tests",
  "scripts": {
    "environment:prepare": "docker-compose -f environment/docker-compose.yml up -d",
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "test": "pnpm run environment:prepare && pnpm run cy:run"
  },
  "devDependencies": {
    "cypress": "12.2.0",
    "typescript": "4.9.4"
  }
}
